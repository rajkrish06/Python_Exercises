
st = input('Enter string to find the vowels count:')
d = {}
for c in st:
    if c in 'aeiouAEIOU':
        d[c] = d.get(c, 0) + 1

print('Number of vowels:{}, Total \
      sum of vowels: {}'.format(len(d.keys()), sum(d.values())))
